import React from 'react';import {ArrowUpRight,MapPin,ShieldCheck} from 'lucide-react';import {useMarketplace} from '../marketplace';
export default function Vendors(){const {vendors}=useMarketplace();const approved=vendors.filter(v=>v.status==='approved');return <main className="page"><section className="page-title"><p className="eyebrow">COMMUNAUTÉ DRIPX+</p><h1>LES BONNES<br/><em>ADRESSES.</em></h1><p>Des vendeurs vérifiés et des créations prêtes à rejoindre votre vestiaire.</p></section><section className="vendors-directory">{approved.map(v=><article className="storefront" key={v.id}><div className="store-no">{v.id}</div><div><span className="verified"><ShieldCheck size={14}/> APPROUVÉ</span><h2>{v.name}</h2><p>{v.description}</p><p className="store-meta"><MapPin size={14}/> Kayes, Mali · {v.pieces}</p></div><button aria-label={`Ouvrir ${v.name}`}><ArrowUpRight/></button></article>)}</section></main>}
